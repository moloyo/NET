@model IEnumerable<Bussiness.Entities.Subject>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WeeklyHs)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalHs)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Planid)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WeeklyHs)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TotalHs)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Planid)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
